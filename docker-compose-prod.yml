version: "2"
services:
  web:
    user: dashboard
    build:
      context: .
    container_name: "id_dashboard"
    restart: always
    links:
      - mongodb
  mongodb:
    image: mongo:2.6
    container_name: "id_dashboard_mongo"
    command: mongod --smallfiles
    restart: always
