<div id="login-menu" >
  <% if (!connected) { %>
  <div class="col-md-5">
    <p>Your OpenMRS ID is the key to participating in our open source
      community. Use it to access all of the tools and services listed
      in the links at the top of this page. <a href="https://wiki.openmrs.org/display/docs/Home">Read more on our Wiki.</a></p>
  </div>
  <% } %>

  <% if (connected) { %>
    <div style="text-align:right;">
      <div class="col-md-4">
        <h2>Welcome, <%= user.displayName %></h2>
        <a href="/disconnect?destination=<%=encodeURIComponent(url || '')%>">
        Log Out</a>
      </div>
      <div class="col-md-1">
        <img src="https://wiki.openmrs.org/rest/cas/1.0/avatar/server/<%= mailHash %>?s=51" alt="<%= user.displayName %>">
      </div>
    </div>
  <% } %>
</div>
